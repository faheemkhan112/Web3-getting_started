//let numbers: number[] = [1, 2, 3, 4, 5];
//console.log(numbers[5]);  // Accessing an index that is out of bounds
var numbers = [1, 2, 3, 4, 5];
console.log(numbers[5]); // Accessing an index that is out of bounds
